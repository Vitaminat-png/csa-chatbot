[pytest]
testpaths = tests
# Explicit markers so `-m "not integration"` actually filters, instead of
# silently matching nothing and running the whole suite.
markers =
    integration: hits OpenAI and Pinecone; needs real API keys
asyncio_mode = strict
filterwarnings =
    ignore::DeprecationWarning
